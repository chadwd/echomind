---
phase: 01.1-ui-polish
plan: 04
type: execute
wave: 3
depends_on:
  - 01.1-02
  - 01.1-03
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "Visual inspection confirms brand purple primary color, not placeholder blue"
    - "App bar logo renders cleanly with no white square artifact"
    - "App looks intentional and on-brand, ready for demo recording"
  artifacts: []
  key_links: []
---

<objective>
Human visual checkpoint — verify the full M3 theme, logo header, and component polish renders correctly in the browser before declaring Phase 1.1 complete.

Purpose: Color values and build success can be grep-verified, but visual fidelity (logo crop, warm white vs flat grey, purple vs blue, sidebar card feel) requires human eyes at browser resolution. This checkpoint is the quality gate before the demo recording.

Output: Chad's sign-off that the app looks on-brand and demo-ready.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-ui-polish/01.1-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Start dev server and verify theme changes look correct</name>
  <what-built>
    Full Phase 1.1 UI polish (Plans 01-03):
    - Plan 01: M3 light scheme applied to Vuetify theme (primary #7A4F81, surface #FFF7FA, surface-variant #ECDFE9, secondary #6B596C, error #BA1A1A)
    - Plan 02: App bar logo polished (left-top crop, no white square, uppercase wordmark)
    - Plan 03: InputPane sidebar card (warm fill surface-container-low), SectionCard finding rows py-3 + lighter divider, empty state icon using secondary color
  </what-built>
  <how-to-verify>
    Start the dev server manually:

    ```
    cd /Users/cdunbar/Repos/echomind && pnpm --filter @echomind/web dev
    ```

    Open the printed Local URL in your browser (typically http://localhost:5173) and verify:

    1. **Color — Primary**: The "Get Feedback" button is purple (#7A4F81), not blue. The stepper active step indicator is purple.

    2. **Color — Surface**: The app background has a warm white tint (not flat #FAFAFA grey). The Configure pane has a slightly warmer/pinker fill vs the results area.

    3. **Logo**: The app bar shows the EchoMind icon mark (brain/head mark) at 40x40 with no white square. The "ECHOMIND" wordmark in uppercase appears next to it. No tagline text visible.

    4. **Configure pane**: Reads as a recessed sidebar, not a generic outlined form. The border is a subtle warm-grey tint (not stark black outline).

    5. **Section cards** (click "Get Feedback" to trigger validation, or check with VITE_REPLAY_MODE=true): Finding rows have generous vertical spacing. Dividers between findings are light and subtle.

    6. **Empty state**: The lightning bolt icon is a muted purple-grey (secondary color), not flat grey.

    7. **Overall**: Does the app look intentional and designed — more Linear/Notion than generic Vuetify scaffold?

    If the logo still shows a white square: replace `apps/web/public/logo-echomind.png` with a transparent-background version and hard-refresh. No code change needed.
  </how-to-verify>
  <resume-signal>
    Type "approved" if the UI looks on-brand and demo-ready.
    Or describe specific issues (e.g., "logo crop shows too much white", "button color looks too dark") and the executor will make targeted fixes before re-verifying.
  </resume-signal>
</task>

</tasks>

<verification>
Human sign-off that:
1. Primary color is visibly purple (not blue)
2. Logo renders without white square artifact
3. Overall UI reads as intentional/designed, not generic Vuetify default
4. App is ready for demo recording
</verification>

<success_criteria>
Chad has visually confirmed the app looks on-brand. Phase 1.1 is complete and the recording can proceed.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-ui-polish/01.1-04-SUMMARY.md`

Also update STATE.md: set Phase 1.1 as complete, set current focus to Phase 2 (Engine Hardening) or note recording is next.
</output>
