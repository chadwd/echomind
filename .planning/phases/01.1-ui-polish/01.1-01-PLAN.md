---
phase: 01.1-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/main.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "The app primary color is #7A4F81 (EchoMind purple), not #1565C0 (placeholder blue)"
    - "The app background and surface use the M3 warm white #FFF7FA, not flat #FAFAFA"
    - "Surface variant uses M3 neutral-purple tint #ECDFE9 for cards and variant surfaces"
    - "Secondary, error, outline, and onSurface roles are set from the M3 light scheme"
  artifacts:
    - path: "apps/web/src/main.ts"
      provides: "Vuetify theme with full M3 light scheme color mapping"
      contains: "#7A4F81"
  key_links:
    - from: "apps/web/src/main.ts"
      to: "Vuetify theme colors"
      via: "createVuetify() themes.light.colors"
      pattern: "primary.*7A4F81"
---

<objective>
Replace the placeholder Vuetify theme in `apps/web/src/main.ts` with the full M3 light scheme from `assets/material-theme-builder/material-theme.json`.

Purpose: Every component that uses Vuetify color tokens (buttons, cards, surfaces, icons) will immediately pick up the EchoMind brand purple and M3 neutral palette without per-component changes. This is the foundation all other Phase 1.1 tasks build on.

Output: `apps/web/src/main.ts` with a complete Vuetify theme mapping the M3 light scheme.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- M3 light scheme values (source: assets/material-theme-builder/material-theme.json) -->
<!-- Executor uses these directly — do not re-read the JSON. -->

Light scheme color roles:
  primary:           "#7A4F81"
  onPrimary:         "#FFFFFF"
  primaryContainer:  "#FDD6FF"
  onPrimaryContainer:"#603768"
  secondary:         "#6B596C"
  onSecondary:       "#FFFFFF"
  secondaryContainer:"#F3DBF2"
  error:             "#BA1A1A"
  onError:           "#FFFFFF"
  errorContainer:    "#FFDAD6"
  background:        "#FFF7FA"
  onBackground:      "#1F1A1F"
  surface:           "#FFF7FA"
  onSurface:         "#1F1A1F"
  surfaceVariant:    "#ECDFE9"
  onSurfaceVariant:  "#4D444C"
  outline:           "#7E747D"
  outlineVariant:    "#CFC3CD"
  surfaceContainerLowest: "#FFFFFF"
  surfaceContainerLow:    "#FCF1F8"
  surfaceContainer:       "#F6EBF2"
  surfaceContainerHigh:   "#F0E5EC"
  surfaceContainerHighest:"#EAE0E7"
  inverseSurface:    "#342F34"
  inverseOnSurface:  "#F9EEF5"
  inversePrimary:    "#E9B5EF"

Vuetify theme key name rules:
  - Vuetify uses camelCase internally but exposes hyphenated CSS vars
  - In createVuetify() colors object, camelCase keys become --v-theme-{kebab-case}
  - Use exact key names: 'primary', 'on-primary', 'primary-container', 'on-primary-container',
    'secondary', 'on-secondary', 'secondary-container', 'error', 'on-error', 'error-container',
    'background', 'on-background', 'surface', 'on-surface', 'surface-variant', 'on-surface-variant',
    'outline', 'outline-variant', 'inverse-surface', 'inverse-on-surface', 'inverse-primary',
    'surface-container-lowest', 'surface-container-low', 'surface-container',
    'surface-container-high', 'surface-container-highest'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply M3 light scheme to Vuetify theme</name>
  <files>apps/web/src/main.ts</files>
  <read_first>
    - apps/web/src/main.ts (current Vuetify bootstrap — read entire file before touching)
  </read_first>
  <action>
Replace the existing `createVuetify()` call's `theme.themes.light.colors` block with the full M3 light scheme mapping below. Keep all non-theme config (icons setup, app mounting) unchanged.

New `colors` object for `themes.light`:

```typescript
colors: {
  primary:                    '#7A4F81',
  'on-primary':               '#FFFFFF',
  'primary-container':        '#FDD6FF',
  'on-primary-container':     '#603768',
  secondary:                  '#6B596C',
  'on-secondary':             '#FFFFFF',
  'secondary-container':      '#F3DBF2',
  error:                      '#BA1A1A',
  'on-error':                 '#FFFFFF',
  'error-container':          '#FFDAD6',
  background:                 '#FFF7FA',
  'on-background':            '#1F1A1F',
  surface:                    '#FFF7FA',
  'on-surface':               '#1F1A1F',
  'surface-variant':          '#ECDFE9',
  'on-surface-variant':       '#4D444C',
  outline:                    '#7E747D',
  'outline-variant':          '#CFC3CD',
  'inverse-surface':          '#342F34',
  'inverse-on-surface':       '#F9EEF5',
  'inverse-primary':          '#E9B5EF',
  'surface-container-lowest': '#FFFFFF',
  'surface-container-low':    '#FCF1F8',
  'surface-container':        '#F6EBF2',
  'surface-container-high':   '#F0E5EC',
  'surface-container-highest':'#EAE0E7',
},
```

The old `'surface-variant': '#F5F5F5'` and `primary: '#1565C0'` lines must be gone. Nothing else in the file changes.
  </action>
  <verify>
    <automated>
grep "#7A4F81" /Users/cdunbar/Repos/echomind/apps/web/src/main.ts && grep "#FFF7FA" /Users/cdunbar/Repos/echomind/apps/web/src/main.ts && grep "#ECDFE9" /Users/cdunbar/Repos/echomind/apps/web/src/main.ts && grep "#BA1A1A" /Users/cdunbar/Repos/echomind/apps/web/src/main.ts && grep "#6B596C" /Users/cdunbar/Repos/echomind/apps/web/src/main.ts && ! grep "#1565C0" /Users/cdunbar/Repos/echomind/apps/web/src/main.ts && ! grep "#FAFAFA" /Users/cdunbar/Repos/echomind/apps/web/src/main.ts && ! grep "#F5F5F5" /Users/cdunbar/Repos/echomind/apps/web/src/main.ts && echo "THEME_OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "#7A4F81" apps/web/src/main.ts` exits 0 (primary purple present)
    - `grep "#FFF7FA" apps/web/src/main.ts` exits 0 (M3 warm white present)
    - `grep "#ECDFE9" apps/web/src/main.ts` exits 0 (surface-variant present)
    - `grep "#BA1A1A" apps/web/src/main.ts` exits 0 (error color present)
    - `grep "#6B596C" apps/web/src/main.ts` exits 0 (secondary color present)
    - `! grep "#1565C0" apps/web/src/main.ts` exits 0 (old blue absent)
    - `! grep "#FAFAFA" apps/web/src/main.ts` exits 0 (old flat surface absent)
  </acceptance_criteria>
  <done>apps/web/src/main.ts has the complete M3 light scheme. The old placeholder blue (#1565C0) and grey surface (#FAFAFA, #F5F5F5) are gone. All Vuetify components in the app now inherit the brand purple and warm neutral palette.</done>
</task>

<task type="auto">
  <name>Task 2: Verify build passes after theme change</name>
  <files></files>
  <read_first>
    - apps/web/src/main.ts (verify it matches what Task 1 wrote before running build)
  </read_first>
  <action>
Run the web build to confirm TypeScript and Vite compile without errors after the theme change. This is a non-transforming verification task — do not edit any files.

Command: `cd /Users/cdunbar/Repos/echomind && pnpm --filter @echomind/web build`

If the build fails, inspect the error — the most likely cause is a malformed colors object (missing comma, extra brace). Fix only `apps/web/src/main.ts` and re-run until build exits 0.
  </action>
  <verify>
    <automated>cd /Users/cdunbar/Repos/echomind && pnpm --filter @echomind/web build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @echomind/web build` exits 0 (no TypeScript or Vite errors)
    - Build output does not contain "error TS" or "ERROR" lines
  </acceptance_criteria>
  <done>The web app builds successfully with the M3 theme applied. No TypeScript or Vite compilation errors.</done>
</task>

</tasks>

<verification>
After both tasks:
- `grep "#7A4F81" apps/web/src/main.ts` returns a match
- `! grep "#1565C0" apps/web/src/main.ts` exits 0 (no matches)
- `pnpm --filter @echomind/web build` exits 0
</verification>

<success_criteria>
The Vuetify theme in main.ts uses the M3 light scheme. EchoMind purple (#7A4F81) is the primary color. Warm white (#FFF7FA) is the surface. The build compiles clean. All subsequent Phase 1.1 plans inherit these tokens automatically.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-ui-polish/01.1-01-SUMMARY.md`
</output>
